# Decision records (ADR)

**What was decided, and the invariant it holds.** One file per decision, written when the mechanism
ships. Append-only: an ADR is never edited to track drift — it is superseded by a later one that
says so.
