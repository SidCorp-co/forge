# Everything in .forge/ is committed — the registry, the baselines, the vendored
# checkers, and the orientation that CLAUDE.md imports on its first line. A fresh
# clone must be held to the same rules as this one, so nothing here is machine-local
# by default, and this file is the only place an exception may be declared.
#
# A trailing comment on a pattern line becomes part of the pattern, so each
# exception gets its own line above it.

# A stale cache of forge_knowledge; the MCP server is the source of truth
# (see CLAUDE.local.md — there is no local knowledge file by design).
knowledge.json

# Anything a tool writes per machine.
*.local.*

# `cm metrics`' event log and its pending-block file, written by the PostToolUse hook on whichever
# machine ran the edit (codemap 0.15.0+, lib/metrics.mjs). Per-session telemetry, not a rule, and
# committing it would conflict on every parallel agent — but unignored it is untracked output that a
# release step's `git status --porcelain` reads as someone's interrupted work.
.codemap-metrics/

# `cm archmap`'s parsed-graph cache and the lock the refresh holds, written by whichever machine ran
# the check (same shape as `.codemap-metrics/` above: per-machine tool output, not a rule). The lock
# carries a pid that is dead on any other box, and the graph is ~800KB that every parallel agent
# rewrites — but unignored it is untracked output a release step's `git status --porcelain` reads as
# someone's interrupted work.
.codemap-archmap-cache/
