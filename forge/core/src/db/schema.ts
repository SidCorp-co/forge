// Drizzle ORM schema — Phase 2.0-C placeholder.
// Tables will be added in later RFC 0002 phases.
// Export an empty object to allow schema-aware tooling (drizzle-kit studio,
// drizzle-kit generate) to run without errors.
export const schema = {};
