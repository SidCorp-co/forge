{
  "$schema": "https://biomejs.dev/schemas/2.5.8/schema.json",
  "vcs": {
    "enabled": true,
    "clientKind": "git",
    "useIgnoreFile": true
  },
  "files": {
    "includes": [
      "**",
      "!**/*.css",
      "!**/.next",
      "!**/coverage",
      "!**/node_modules"
    ]
  },
  "formatter": {
    "enabled": false
  },
  "assist": {
    "actions": {
      "source": {
        "organizeImports": "off"
      }
    }
  },
  "linter": {
    "enabled": true,
    "rules": {
      "preset": "recommended",
      "style": {
        "noNonNullAssertion": "warn",
        "noRestrictedImports": {
          "level": "error",
          "options": {
            "paths": {
              "@floating-ui/react": "@floating-ui/react is a behaviour library web-v2 keeps behind its own design system (ISS-1172). Import it only inside src/design/**, and give features a component or hook from @/design that passes their own types, never the library's.",
              "react-hook-form": "react-hook-form is a behaviour library web-v2 keeps behind its own design system (ISS-1172). Import it only inside src/design/**, and give features a component or hook from @/design that passes their own types, never the library's.",
              "@hookform/resolvers": "@hookform/resolvers is a behaviour library web-v2 keeps behind its own design system (ISS-1172). Import it only inside src/design/**, and give features a component or hook from @/design that passes their own types, never the library's.",
              "@tanstack/react-table": "@tanstack/react-table is a behaviour library web-v2 keeps behind its own design system (ISS-1172). Import it only inside src/design/**, and give features a component or hook from @/design that passes their own types, never the library's.",
              "@tanstack/react-virtual": "@tanstack/react-virtual is a behaviour library web-v2 keeps behind its own design system (ISS-1172). Import it only inside src/design/**, and give features a component or hook from @/design that passes their own types, never the library's."
            },
            "patterns": [
              {
                "group": [
                  "@floating-ui/react/**",
                  "react-hook-form/**",
                  "@hookform/resolvers/**",
                  "@tanstack/react-table/**",
                  "@tanstack/react-virtual/**"
                ],
                "message": "A subpath of this package is a behaviour library web-v2 keeps behind its own design system (ISS-1172). Import it only inside src/design/**, and give features a component or hook from @/design that passes their own types, never the library's."
              }
            ]
          }
        }
      },
      "suspicious": {
        "noExplicitAny": "warn"
      }
    }
  },
  "overrides": [
    {
      "includes": [
        "src/design/**"
      ],
      "linter": {
        "rules": {
          "style": {
            "noRestrictedImports": "off"
          }
        }
      }
    }
  ]
}
