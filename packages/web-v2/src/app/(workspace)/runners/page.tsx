"use client";

import { RunnersScreen } from "@/features/runners/components/runners-screen";

/** `/runners` — paired devices + browser-approve pairing surface (ISS-305). */
export default function RunnersPage() {
  return <RunnersScreen />;
}
