"use client";

import { DocsScreen } from "@/features/docs/components/docs-screen";

/** `/docs` — project markdown docs tree + content + search + TOC (ISS-305). */
export default function DocsPage() {
  return <DocsScreen />;
}
