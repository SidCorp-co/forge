"use client";

// Workspace-tier Usage index (`/usage`, ISS-359) — cross-project token-spend
// overview. Replaces the removed Activity destination.
import { UsageScreen } from "@/features/usage/components/usage-screen";

export default function UsagePage() {
  return <UsageScreen />;
}
