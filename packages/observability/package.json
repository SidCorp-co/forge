{
  "name": "@forge/observability",
  "version": "0.0.1",
  "private": true,
  "license": "Apache-2.0",
  "description": "Shared observability primitives — privacy/PII scrubbing constants used by every Sentry adapter (dev renderer, core, web). Single source of truth so the privacy contract can't drift across surfaces.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "types": "./src/index.ts",
  "files": [
    "src"
  ]
}
