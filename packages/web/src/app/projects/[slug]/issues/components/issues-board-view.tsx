'use client';

// ISS-42: kanban board view depends on BoardCard which is stubbed pending
// the A1 follow-up. Restored together with BoardCard once IssuesView wires
// `viewMode === 'board'`.
export function IssuesBoardView(): null {
  return null;
}
