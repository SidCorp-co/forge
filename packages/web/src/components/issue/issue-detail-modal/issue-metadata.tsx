'use client';

// ISS-42: full IssueMetadata header lives inside the issue detail page now;
// the modal it belonged to is itself a stub (issue-detail-modal.tsx).
// Restored as part of the A2 detail-page rewrite.
export function IssueMetadata(): null {
  return null;
}
